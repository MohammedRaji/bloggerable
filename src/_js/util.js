/**
 * --------------------------------------------------------------------------
 * Public Util
 * --------------------------------------------------------------------------
 */

const Util = {
  square(number) {
    return number * number;
  },
  multiply(x, y) {
    return x * y;
  }
}

export default Util;
